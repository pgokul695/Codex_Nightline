{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for Schedger's desktop window",
  "windows": ["main"],
  "permissions": ["core:default", "fs:default", "shell:allow-open"]
}
